[phases.setup]
nixPkgs = ["nodejs_20", "ffmpeg"]
cmds = ["echo 'Build timestamp: $(date)' > build_timestamp.txt"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["cd Student_summary && npm run build"]

[start]
cmd = "node server/server.js"

# Force rebuild: 2025-05-11-005 